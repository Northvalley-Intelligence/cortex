# Copy to .env.local and fill in. .env.local is gitignored and must never be committed.
# Source of truth for these keys: mde-control-center/.mde/control-center/secrets.env
GEMINI_API_KEY=
OPENROUTER_API_KEY=
